# 一、排序数据
## 1.1 排序guize
如果没有使用排序操作，默认情况下查询返回的数据是按照添加数据的顺序显示的。

> * 使用 ORDER BY 子句排序 -order(排序)
> 	* ASC（ascend）: 升序
> 	* DESC（descend）:降序 
> * ORDER BY 子句在SELECT语句的结尾。
> * 如果在 TRDET BY 后没有使用显示指明排序的方式的话，则默认按照升序排列。 
## 1.2 单列排序
 按照啊salary从高到低(降序)的顺序显示员工信息
 ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1712039319000f5i84o.png)
 那么是否可以使用[[04 基本的SELECT语句#3.2 别名(alias)|别名]]呢？
 ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1712039576000rwe0da.png)
> 这样就可以知道，列的别名只能在order by 中使用，不能在 where 中使用。

### 1.2.1 WHERE 和 ORDET BY 联合使用
> [!tip] 格式强调
> * WHERE 需要声明在 FROM 后面，ORDET BY 之前。
> * 代码是先执行FROM->WHERE->SELECT->ORDER BY
> 	* 所以如果在WHERE中使用了字段的别名，这里还没有运行到SELECT设置字段的别名，不知道这个是那个字段，所以就会报错
> 	* 运行到ORDER BY 的时候已经运行了SELECT 所以就可以使用字段的别名了。
> ```
> SELECT
> FROM
> WHERE
> ORDER BY
> ```

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/171204038400038j61i.png)

## 1.3 多列排序
显示员工信息，按照department_id的降序排列，salary的升序排列：

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1712040794000vymn0r.png)


# 二、分页
## 2.1 背景
背景1：查询返回的记录太多了，查看起来很不方便，怎么样能够实现分页查询呢？ 
背景2：表里有 4 条数据，我们只想要显示第 2、3 条数据怎么办呢？
## 2.2 实现规则
MySQL中使用link来实现分页：
```
LIMIT [位置偏移量,行数] 
LIMIT 0,行数 == LIMIT 行数
limit - 限制；界限；范围
```

练习：
1. 每页显示20条数据，此时显示第3页。

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17120419610009ti95f.png)

> * MySQL 8.0中可以使用“LIMIT 3 OFFSET 4”，意思是获取从第5条记录开始后面的3条记录，和“LIMIT 4,3;”返回的结果相同。
> * LIMIT 3 OFFSET 4 == LIMIT 4,3
> * offset:低效、补偿、出端。
### 2.2.1 WHERE 和 RODER BY 和 LIMIT 声明
声明顺序如下：
```
SELECT
FROM
WHERE
ORDER BY
LIMIT
```
## 2.3 拓宽
在不同的 DBMS 中使用的关键字可能不同。在 MySQL、PostgreSQL、MariaDB 和 SQLite 中使用 LIMIT 关 键字，而且需要放到 SELECT 语句的最后面。

* 在不同的 DBMS 中使用的关键字可能不同。在 MySQL、PostgreSQL、MariaDB 和 SQLite 中使用 LIMIT 关 键字，而且需要放到 SELECT 语句的最后面。
```
SELECT TOP 5 name, hp_max FROM heros ORDER BY hp_max DESC
```

* 如果是 DB2，使用 FETCH FIRST 5 ROWS ONLY 这样的关键字：
```
SELECT name, hp_max FROM heros ORDER BY hp_max DESC FETCH FIRST 5 ROWS ONLY
```

* 如果是 Oracle，你需要基于 ROWNUM 来统计行数：
```
SELECT rownum,last_name,salary FROM employees WHERE rownum < 5 ORDER BY salary DESC;
```

需要说明的是，这条语句是先取出来前 5 条数据行，然后再按照 hp_max 从高到低的顺序进行排序。但 这样产生的结果和上述方法的并不一样。我会在后面讲到子查询，你可以使用
```
SELECT last_name,salary   
FROM employees   
ORDER BY salary DESC) 
WHERE rownum < 10;
```
得到与上述方法一致的结果。
