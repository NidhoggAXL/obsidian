```js
<script>
  function Person(name, age) {
    this.name = name
    this.age = age
  }

  //添加到函数的显式原型对象上的是 实例方法
  Person.prototype.running = function() {}
  var stu = new Person("axl", 19)
  console.log(stu)
  //Person {name: 'axl', age: 19}

  //添加到函数对象属性上方法叫做 类方法
  Person.eatting = function() {
    return "eatting"
  }

//执行函数返回 ratting 赋值给 eatting
  var p = Person.eatting()
  console.log(p)//eattingh
</script>
```

