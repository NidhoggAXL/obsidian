**首相要明白为什么会存在间隙：**

因为我们在编写代码的时候，为了方便代码的阅读，会进行换行操作。浏览时在解析的时候就会把换行当作空格。

**解决的办法如下：**

* 将 父级元素 的 font-size:0; ，但是需要设置子元素的 font-size **（不推荐）**。
	* 原理：就是空格也是一种 font-size ，所以设置了 font-size:0;这样空格就没有大小啦，所以空格就不存在啦。
* 子元素统一向一个方向浮动。
* flex 布局 **(使用最多的)** 。








