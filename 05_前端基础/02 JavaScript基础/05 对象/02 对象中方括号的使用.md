使用**字符串属性**的时候，就要使用到方括号

```js
var bar = {
  name: "why",
  "my name": "kobe"
}
  
console.log(bar["my name"])//kobe
```


这是因为点符号要求 key 是有效的变量标识符

* 不包含空格，不以数字开头，也不包含特殊字符(允许使用 `$_` 和 `_` );

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1727528763000oznjzm.png)


