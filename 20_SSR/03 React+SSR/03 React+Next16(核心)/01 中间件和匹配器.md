# 一、中间件

Next.js的中间件允许我们去拦截客户端发起的请求，例如：API请求、router切换、资源加载、站点图片等。

拦截客服端发起的请求等等之后，便可对这些进行：重写、重定向、修改请求响应标头、或响应等操作。

使用中间件需按照以下步骤操作：

1. 在**根目录中创建 proxy.ts 文件**
2. 从 proxy.ts 文件中导出一个中间件 proxy函数（支持 async，并只允许在服务端），会接收两个参数： 
	- req：类型为 NextRequest 
	- event：类型为 NextFetchEvent
3. 通过返回Response对象来实现重定向等功能 
	- next()- 将继续中间件链 
	- redirect()- 将重定向，如：重定向到某个页面 
	- rewrite()- 将重写URL，如：配置反向代理
4. 没返回值：页面将按预期加载 和 返回 next() 一样

```ts
import { NextRequest, NextFetchEvent, NextResponse } from 'next/server'

export function proxy(request: NextRequest, event: NextFetchEvent) {
  // 你的中间件逻辑
  return NextResponse.next()
}
```

## 1.1 参数说明

| 参数        | 类型               | 说明                                                                                                                   |
| --------- | ---------------- | -------------------------------------------------------------------------------------------------------------------- |
| `request` | `NextRequest`    | 传入的 HTTP 请求对象，包含 URL、headers、cookies 等信息                                                                             |
| `event`   | `NextFetchEvent` | 扩展自原生 `FetchEvent`，提供 `waitUntil()` 方法[](https://nextjscn.org/docs/app/api-reference/file-conventions/proxy#matcher) |

## 1.2 `NextFetchEvent` 的主要用途

`waitUntil()` 方法接受一个 Promise，可以**延长 proxy 的生命周期**直到该 Promise 完成。这对于在后台执行异步任务非常有用，而不会阻塞响应返回[](https://nextjscn.org/docs/app/api-reference/file-conventions/proxy#matcher)。

```ts
import { NextRequest, NextFetchEvent, NextResponse } from 'next/server'

export function proxy(request: NextRequest, event: NextFetchEvent) {
  // 记录请求日志（后台执行，不阻塞响应）
  event.waitUntil(
    logToAnalytics(request.url, request.headers.get('user-agent'))
  )
  
  // 继续处理请求，立即返回响应
  return NextResponse.next()
}

async function logToAnalytics(url: string, userAgent: string | null) {
  // 异步发送日志到外部服务
  await fetch('https://analytics.example.com/log', {
    method: 'POST',
    body: JSON.stringify({ url, userAgent, timestamp: new Date() })
  })
}
```

> [!tip] 注意事项
 > - event.waitUntil() 适合执行非关键的后台任务，如日志记录、监控上报等
 > - proxy 函数应该保持轻量，避免在 waitUntil 之外执行耗时操作
 > - proxy 默认运行在 Node.js 运行时
 
# 二、匹配器（Matcher）

匹配器允许我们**过滤中间件以在特定路径上运行**，比如：

 - `matcher: ‘/about/:path*`， 意思是匹配以 `/about/*` 开头的路径。其中路径开头的：是修饰符， 而 `* 代表0个 或 n个
 - `matcher: [‘/about/:path*’, ‘/dashboard/:path*’]` ， 意思是匹配以` /about/*` 和 `/dashboard/*` 开头的路径
 - `matcher: [‘/((?!api|_next/static|favicon.ico).*)’]` ，意思是不匹配以 `api、_next、static、favicon.ico` 开头的路径

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1774538127000ptoo13.png)


> [!tip] 注意：上面的 path 是占位符， 不是固定的。

# 三、路由拦截

下面我们通过 中间件 + 匹配器 来实现路由的拦截：

 - `matcher: ['/((?!api|_next/static|favicon.ico).*)']`

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1774538268000ujimq5.png)







