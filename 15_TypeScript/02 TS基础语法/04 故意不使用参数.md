使用下划线 `_` 作为未使用参数的前缀，这是 TypeScript/JavaScript 中的常见约定，表示这个参数是故意不使用的：

```js
// _prev 就是故意不使用的参数，这样 TS 就不会报错啦。
function bannerBeforeChangeAction(_prev: number, next: number) {
  setCurrent(next)
}
```

