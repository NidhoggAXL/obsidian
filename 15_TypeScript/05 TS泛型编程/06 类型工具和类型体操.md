# 一、类型体操

类型系统其实在很多语言里面都是有的，比如Java、Swift、C++等等，但是相对来说TypeScript的类型非常灵活：

- 这是因为TypeScript的目的是为**JavaScript添加一套类型校验系统**，因为JavaScript本身的灵活性，也让TypeScript类型系统**不得不增加更附加的功能以适配JavaScript的灵活性**；
- 所以TypeScript是一种可以**支持类型编程的类型系统**；

这种类型编程系统为TypeScript增加了很大的灵活度，同时也增加了它的难度：

- 如果你不仅仅在开发业务的时候为自己的JavaScript代码增加上类型约束，那么基本不需要太多的类型编程能力；
- 但是如果你在开发一些框架、库，或者通用性的工具，为了考虑各种适配的情况，就需要使用类型编程；

TypeScript本身为我们提供了类型工具，帮助我们辅助进行类型转换（前面有用过关于this的类型工具）。

很多开发者为了进一步增强自己的TypeScript编程能力，还会专门去做一些类型体操的题目：

- https://github.com/type-challenges/type-challenges
- https://ghaiklor.github.io/type-challenges-solutions/en/

# 二、内置类型工具

## 2.1 Partial`<Type>`

**partial(部分)**，用于构造一个 Type下面的所有属性都设置为**可选的类型**: 构造一个让 IPerson 类型下面的所有属性都设置为可选属性

```ts
interface IPerson {
  name: string;
  age: number;
  gender?: string;
}

//使用内置工具
type PartialPerson = Partial<IPerson>;
/**partialPeron类型
type PartialPerson = {
  name?: string | undefined;
  age?: number | undefined;
  gender?: string | undefined;
}
 */

//自定义工具(使用映射)
type MyPartial<T> = {
  [P in keyof T]?: T[P];
}
type MyPartialPerson = MyPartial<IPerson>;
/**
type MyPartialPerson = {
  name?: string | undefined;
  age?: number | undefined;
  gender?: string | undefined;
}
 */
```

## 2.2 Required`<Type>`

**required(必需的)**，用于构造Type下面的所有属性全都设置为**必填的类型**，这个工具类型跟 Partial 相反。一个

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1760863280000inca3w.png)

## 2.3 Readonly`<Type>`

用于构造一个Type下面的所有属性全都设置为**只读的类型**，意味着这个类型的**所有的属性全都不可以重新赋值。**

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1760864199000cni4gx.png)

## 2.4 Record<Keys,Type>

用于构造一个对象类型，它所有的key(键)都是Keys类型，它所有的value(值)都是Type类型。

首先要知道下面语法的结果：

```ts
type type = keyof any
//type type = string | number | symbol
```

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1760864607000b4tsjb.png)


## 2.5 Pick<Type,keys>

用于构造一个类型，它是从Type类型里面挑了一些属性Keys

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/17608663310006uwlso.png)


## 2.6 Omit<Type, Keys>

用于构造一个类型，它是从Type类型里面过滤了一些属性Keys

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/17608664580006le8mc.png)

## 2.7 Exclude<UnionType, ExcludedMembers>

用于构造一个类型，它是从UnionType联合类型里面排除了所有可以赋给ExcludedMembers的类型。**利用了[[05 TS条件类型#三、分发条件类型(Distributive condition Types)|分发]]**。


![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1760866589000zdp60x.png)

有了HYExclude，我们可以使用它来实现HYOmit。


## 2.8 Extract<Type, Union>

用于构造一个类型，它是从Type类型里面提取了所有可以赋给Union的类型。**利用了[[05 TS条件类型#三、分发条件类型(Distributive condition Types)|分发]]**。

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/176086662100081mvmc.png)


## 2.9 NonNullable`<Type>`

于构造一个类型，这个类型从Type中排除了所有的null、undefined的类型。

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1760866696000wmqcur.png)

## 2.10 ReturnType`<Type>`

用于构造一个含有Type函数的返回值的类型。(使用了[[05 TS条件类型#二、条件类型中推断(infer)| infer 关键字]])

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1760866725000pacgqn.png)

## 2.11 InstanceType`<Type>`

用于构造一个由所有Type的构造函数的实例类型组成的类型。

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1760867204000i61l5t.png)

HYInstanceType，自定义的实现(**[[05 TS条件类型#二、条件类型中推断(infer)|使用infer]]**)：

```ts
type HYInstanceType<T extends new (...args: any[]) => any> =  T extends new (...args: any[]) => infer R ? R : never
```








