# 一、认识UI库
UI库的全称是User Interface 库，中文译为用户界面库，是一个预先编写好的用户界面组件集合。这些组件可以被开发者用于快速构建具有特定风格和功能的应用程序界面，从而提高开发效率和设计一致性

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1748958974000yr43rg.png)



# 二、 vant UI库

vant 使用首先需要安装：

```
# Vue 3 项目，安装最新版 Vant
npm i vant

# Vue 2 项目，安装 Vant 2
npm i vant@latest-v2

# 通过 yarn 安装
yarn add vant

# 通过 pnpm 安装
pnpm add vant
```



安装好以后还需要进行一个配置：

在基于 `vite`、`webpack` 或 `vue-cli` 的项目中使用 Vant 时，可以使用 [unplugin-vue-components](https://github.com/antfu/unplugin-vue-components) 插件，它可以自动引入组件，并按需引入组件的样式。

相比于常规用法，这种方式可以按需引入组件的 CSS 样式，从而减少一部分代码体积，但使用起来会变得繁琐一些。如果业务对 CSS 的体积要求不是特别极致，我们推荐使用更简便的常规用法。

## 1. 安装插件
这个UI库，可以按需引入，就不会存在包很大的情况，不过需要安装一个插件才可以：

```
# 通过 npm 安装
npm i unplugin-vue-components -D

# 通过 yarn 安装
yarn add unplugin-vue-components -D

# 通过 pnpm 安装
pnpm add unplugin-vue-components -D

```

## 2. 配置插件

如果是基于 `vite` 的项目，在 `vite.config.js` 文件中配置插件：

```js
import vue from '@vitejs/plugin-vue';
import Components from 'unplugin-vue-components/vite';
import { VantResolver } from 'unplugin-vue-components/resolvers';

export default {
  plugins: [
    vue(),
    Components({
      resolvers: [VantResolver()],
    }),
  ],
};
```

如果是基于 `vue-cli` 的项目，在 `vue.config.js` 文件中配置插件：

```js
const { VantResolver } = require('unplugin-vue-components/resolvers');
const ComponentsPlugin = require('unplugin-vue-components/webpack');

module.exports = {
  configureWebpack: {
    plugins: [
      ComponentsPlugin({
        resolvers: [VantResolver()],
      }),
    ],
  },
};
```

如果是基于 `webpack` 的项目，在 `webpack.config.js` 文件中配置插件：

```js
const { VantResolver } = require('unplugin-vue-components/resolvers');
const ComponentsPlugin = require('unplugin-vue-components/webpack');

module.exports = {
  plugins: [
    ComponentsPlugin({
      resolvers: [VantResolver()],
    }),
  ],
};
```

## 3. 使用组件

完成以上两步，就可以直接在模板中使用 Vant 组件了，`unplugin-vue-components` 会解析模板并自动注册对应的组件

```html
<template>
  <van-button type="primary" />
</template>
```

## 4. 引入函数组件的样式

Vant 中有个别组件是以函数的形式提供的，包括 `Toast`，`Dialog`，`Notify` 和 `ImagePreview` 组件。在使用函数组件时，`unplugin-vue-components` 无法自动引入对应的样式，因此需要手动引入样式。

```js
// Toast
import { Toast } from 'vant';
import 'vant/es/toast/style';

// Dialog
import { Dialog } from 'vant';
import 'vant/es/dialog/style';

// Notify
import { Notify } from 'vant';
import 'vant/es/notify/style';

// ImagePreview
import { ImagePreview } from 'vant';
import 'vant/es/image-preview/style';
```

你可以在项目的入口文件或公共模块中引入以上组件的样式，这样在业务代码中使用组件时，便不再需要重复引入样式了。

> [!note] 提示：在单个项目中不应该同时使用「全量引入」和「按需引入」，否则会导致代码重复、样式错乱等问题。

## 5. 修改UI样式

### 5.1 使用插槽情况

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749042328000i9lth4.png)

在 vant UI库里面使用 vant-tabbar-item 组件，要修改里面自己添加 img 的样式，直接在本组件里面修改 img 的样式(<mark class="hltr-orange">在自己的作用域里面</mark>）。

```less
.tab-bar {
	img {
		width: 36px;
	}
}
```

### 5.2 修改默认的组件
使用了默认的图标，要修改的话有四种方法

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749043244000pr2agu.png)


![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749043587000gankdy.png)

先来看看，默认的样式是如何来的。

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17490435580003pqtsd.png)

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749043475000ca7dxt.png)


可以看到是图标是一个[[03 web 字体图标|字体图标]]，定义一个全局CSS的变量样式，对 .van-tabbar-item_icon 修改 font-size 来进行修改字体图标。

#### 全局覆盖默认变量值

定义一个全局图标来覆盖默认定义的 font-size，因为这个项目是引入过 common.css 这个文件，这个里面重新定义变量覆盖。

* !important 来确定优先级

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749043977000b54scg.png)

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/174904400600011cryh.png)

> [!tip] 缺点：
> 如果这样全局修改了变量值，那么在其他组件里面使用的时候也会被修改掉，
> 会影响其他的组件。

#### 局部覆盖默认变量值

在自己组件里面修改了变量的值进行覆盖，也可以改变

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749044160000iod3dg.png)

> [!tip] 优点
> 这样的局部修改影响其他的组件。


#### 选择器修改

对选择器（van-tabbar-item__icon）直接修改：

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749044347000g5uvos.png)

会发现不起作用，产生的原因是 style 的作用域问题：

1. van-tabbar-item__icon 在自己组件里面是没有这个选择器

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749044439000yllgkq.png)

2. 居然没有这个选择器，那么就更定不起作用啦。

> 那么这个选择器（van-tabbar-item__icon）到底是哪一个选择器的呢？
> 这个选择器是在 van-tabbar-item 组件里面的选择器，自己组件和 van-tabbar-item 组件都添加了 [[06 单文件style作用域|单文件style总用域]]，所以是不起作用的。

3. 使用Vue的 deep() 来深入到子组件修改，把选择器作为参数传入

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749044810000ms3qj9.png)

#### 总结

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749044855000o7fhde.png)


## 6. 隐藏根组件中某些内容

#### 6.1 通过路由添加参数

在 路由 中为 city 添加参数，在通过 [[06 Vue的条件渲染#3.1 v-show和v-if的区别|v-if]] 来对路由参数进行判断来改变在哪一个路由中要添加根组件的内容

路由中[[03 路由懒加载分包处理#三、路由的其他属性|添加参数]]：

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1749124619000o9o5xb.png)

在需要动态显示的组件中，通过[[04 动态路由和路由嵌套#二、获取动态路由的值|useRoute]]来获取路由的其它信息：





