# 一、代码的页面跳转
有时候我们希望通过代码来完成页面的跳转，比如点击的是一个按钮：

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/174774577200015h92i.png)

当然，我们也可以传入一个对象：

```js
function WebSwitch() {
  router.push({
    path: '/home',
  })
}
```

如果再 opsition API 里面使用的话：

```js
methods: {
	WebSwitch() {
		this.$router.push('/home')
	}
}
```

# 二、query方式的参数

也可以通过query的方式来传递参数：

```js
function WebSwitch() {
  router.push({
    path: '/home',
    //使用query传递参数
    query: {
      id: 123,
      name: 'zhangsan'
    }
  })
}
```

```url
http://localhost:8080/#/home/product?id=123&name=zhangsan
```

> 也可以不使用 path 路径来路由，可以使用 name 来进行路由`name: 'home'`,但是这样的话就不会有上面这种 URL 来显示了，<mark class="hltr-orange">开发中一般使用 path 来进行路由</mark>

在跳转目标的界面中通过 $route.query 来获取参数：

```html
<h2>query: {{ $route.query.name }} - {{ $route.query.id }}</h2>
```

# 三、替换当前的位置
使用push的特点是压入一个新的页面，那么在用户点击返回时，上一个页面还可以回退，但是如果我们希望当前页面是一个替换操作，那么可以使用replace：

| 声明式                               | 编程式                   |
| --------------------------------- | --------------------- |
| `<router-link :to="..." replace>` | `router.replace(...)` |

# 四、页面的前进后退
router的go方法：

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17477463250007gpyhs.png)

**router也有back：** 

* 通过调用 history.back() 回溯历史。相当于 router.go(-1)； 

**router也有forward：** 

* 通过调用 history.forward() 在历史中前进。相当于 router.go(1)；


