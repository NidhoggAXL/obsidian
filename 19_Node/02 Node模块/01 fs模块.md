# 一、内置fs模块

fs是File System的缩写，表示文件系统。

对于任何一个为服务器端服务的语言或者框架通常都会有自己的文件系统：

 - 因为服务器需要将各种数据、文件等放置到不同的地方；
 - 比如**用户数据可能大多数是放到数据库中**；
 - 比如某些配置文件或者用户资源（图片、音视频）都是以**文件的形式存在于操作系统上**的；

Node也有自己的文件系统操作模块，就是fs：

 - 借助于Node帮我们封装的文件系统，我们可以**在任何的操作系统**（window、Mac OS、Linux）上面直接去操作文件；
 - 这也是**Node可以开发服务器的一大原因**，也是它可以成为前端自动化脚本等热门工具的原因；

# 二、fs的API

Node文件系统的API非常的多：

 - https://nodejs.org/docs/latest-v16.x/api/fs.html
 - 不可能，**也没必要一个个去学习**；
 - 这个更多的应该是作为一个API查询的手册，等用到的时候查询即可；
 - 学习阶段我们只需要学习最常用的即可；

但是这些API大多数都提供三种操作方式：

 - **方式一**：同步操作文件：代码会被阻塞，不会继续执行；
 - **方式二**：异步回调函数操作文件：代码不会被阻塞，需要传入回调函数，当获取到结果时，回调函数被执行；
 - **方式三**：异步Promise操作文件：代码不会被阻塞，通过 fs.promises 调用方法操作，会返回一个Promise，可以通过then、catch进行处理；

## 案例

### 获取一个文件的状态

方式一：同步操作文件

```js
import fs from 'fs'

const res1 = fs.readFileSync("./text.txt")
console.log(res1)
// 获取到是十进制，只是方便观看，显示了16进制
// <Buffer 32 30 32 35 e5 b9 b4 31 32 e6 9c 88 37 e6 97 a5 e5 bc 80 e5 a7 8b e5 ad a6 e4 b9 a0 4e 6f 64 65 2e 6a 73>
```

> [!tip]
> 这里使用的是module的导入方式，使用CommonJS导入也是可以的。

但是会发现打印出来的结果是看不懂的，那么可以使用 readFileSync 的**第二个参数**，或者对结果进行 toString 操作

```js
const res1 = fs.readFileSync("./text.txt", {
  encoding: "utf-8"
})
console.log(res1)
//2025年12月7日开始学习Node.js

const res2 = fs.readFileSync("./text.txt")
console.log(res2.toString())
//2025年12月7日开始学习Node.js
```

方式二：异步回调函数操作文件

```js
const res1 = fs.readFile("./text.txt", {
  encoding: "utf-8"
}, (err, data) => {
  if (err) {
    console.log(err)
    return
  }
  console.log(data)
})
console.log("-----")
// -----
// 2025年12月7日开始学习Node.js
```

方式三：异步Promise操作文件

```js
const res1 = fs.promises.readFile("./text.txt", { encoding: "utf-8" })
res1.then(data => {
  console.log(data)
}).catch(err => {
  console.log(err)
})
console.log("-----")
// -----
// 2025年12月7日开始学习Node.js
```

# 四、文件描述符

文件描述符（File descriptors）是什么呢？

 - 在常见的操作系统上，对于每个进程，内核都**维护着一张当前打开着的文件和资源的表格**。
 - 每个打开的文件都**分配了一个称为文件描述符的简单的数字标识符**。
 - 在系统层，**所有文件系统操作**都使用这些文件描述符来标识和跟踪每个特定的文件。
 - <mark class="hltr-cyan">Windows 系统使用了一个虽然不同但概念上类似的机制来跟踪资源</mark>。

为了简化用户的工作，Node.js 抽象出操作系统之间的特定差异，并为所有打开的文件分配一个数字型的文件描述符。

> [!abstract] 补充
> 1.  文件描述符
> 2.  文件描述符是一个非负整数，唯一标识一个打开的文件
> 3.  文件描述符从3开始，0、1、2分别代表标准输入、标准输出、标准错误输出
> 4.  文件描述符可以用来读取、写入文件
> 5.  文件描述符可以用来创建管道、套接字等

`fs.open()` 方法用于分配新的文件描述符。

 - 一旦被分配，则文件描述符可用于**从文件读取数据、向文件写入数据、或请求关于文件的信息**。

> [!tip]
> 当然数据 fs 的 API 也存在三种操作模式。



```js
import fs from "fs";

const res = fs.open("./text.txt", (err, fd) => {
  if (err) return;

  // 获取文件描述符
  console.log(fd); //3

  // 获取文件信息
  fs.fstat(fd, (err, data) => {
    if (err) return;
    // 回调函数里面获取到文件的信息
    console.log(data);

    // 手动关闭文件
    fs.close(fd);
  });
});
```


![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/1765980381000qwvaef.png)

# 五、文件的读写

如果我们希望对文件的内容进行操作，这个时候可以使用文件的读写：

 - `fs.readFile(path[, options], callback)`：读取文件的内容；
 - `fs.writeFile(file, data[, options], callback)`：在文件中写入内容；

```js
import fs from 'fs'

fs.writeFile("./aaa.txt", "hello world", (err) => {
  if (err) return;
  console.log("写入成功");
})
```

如果没有 aaa.txt 文件，会创建这个文件，并写入 `hello world`。

writeFile 总共有4个参数：

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/176603452700007021x.png)

会发现有一个对象类型，这个是写入时填写的option参数：

 - flag：写入的方式。
 - encoding：字符的编码；

## 5.1 flag选项

flag的值有很多：https://nodejs.org/dist/latest-v14.x/docs/api/fs.html#fs_file_system_flags

- **w** (默认) - 打开文件用于写入。**文件不存在则创建**，存在则截断（清空内容），<mark class="hltr-cyan">覆盖原本的内容。</mark>
    
- **wx** 类似 `'w'`，但文件存在时失败。
	
- **w+** 打开用于读写。**文件不存在则创建**，存在则截断。
    
- **a** 打开文件用于追加。**文件不存在则创建**，存在则在末尾追加。
	
-  **a+** 打开文件以进行读写（可读可写），将流放在文件末尾。如果不存在则创建文件
    
- **ax** 类似 `'a'`，但文件存在时失败。
    
- **r** 只读打开。**读取时的默认值，文件不存在则报错**。
	
 - **r+** 打开文件进行读写，**如果不存在那么抛出异常**；

## 5.2 encoding选项

再来看看编码：

 - coderWhy在简书上写过一篇关于字符编码的文章：https://www.jianshu.com/p/899e749be47c
 - 目前基本用的都是UTF-8编码；

**文件读取**：如果不填写encoding，返回的结果是Buffer；

```js
fs.readFile("./text.txt", { encoding: "utf-8" }, (err, data) => {
  if (err) return;
  console.log(data);
});
```

# 六、文件夹操作

## 6.1 创建

新建一个文件夹：使用fs.mkdir()或fs.mkdirSync()创建一个新文件夹：

```js
import fs from "fs";

fs.mkdir("axl", (err) => {
  if (err) {
    console.log(err);
  } else {
    console.log("创建文件夹成功");
  }
});

```

## 6.2 读取

```js
import fs from "fs";

// 文件夹读取到，获取的是一个字符串数组
fs.readdir("./axl", (err, data) => {
  console.log(data); //[ 'aaa', 'abc.txt', 'bbb' ]
});
```

> [!abstarct]
> - 分辨不了文件还是文件夹
> - 获取不到文件的信息
> - 子文件夹里面的内容获取不到


```js
import fs from "fs";

// 读取文件夹，获取文件信息
fs.readdir("./axl", { withFileTypes: true }, (err, file) => {
  console.log(file)// 获取到的是一个数组
});
```

![gh|300](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2025/17660366310002kk1am.png)

> [!abstract]
> - 分辨不了文件和文件夹
> - 子文件内容获取不到

```js
import fs from "fs";


// 读取文件夹，获取文件信息
fs.readdir("./axl", { withFileTypes: true }, (err, file) => {
  file.forEach(item => {
    // 判断是否为一个文件夹
    if (item.isDirectory()) {
      console.log(item)//文件夹信息
      console.log(item.name + "是一个文件夹");
    } else {
      console.log(item)//文件信息
      console.log(item.name + "是一个文件");
    }
  })
})

// Dirent {
//   name: 'aaa',
//   parentPath: './axl',
//   path: './axl',
//   [Symbol(type)]: 2
// }
// aaa是一个文件夹
// Dirent {
//   name: 'abc.txt',
//   parentPath: './axl',
//   path: './axl',
//   [Symbol(type)]: 1
// }
// abc.txt是一个文件
// Dirent {
//   name: 'bbb',
//   parentPath: './axl',
//   path: './axl',
//   [Symbol(type)]: 2
// }
// bbb是一个文件夹
```

> [!abstarct]
> - 获取不到子文件的内容

```js
import fs from "fs";

// 读取文件夹，获取文件信息
fs.readdir("./axl", { withFileTypes: true }, (err, file) => {
  file.forEach((item) => {
    if (item.isDirectory()) {
      console.log(item.name + "是一个文件夹");
      // 获取文件夹里面的内容
      fs.readdir(
        `./axl/${item.name}`,
        { withFileTypes: true },
        (err, file) => {
          console.log(file);
        }
      );
    } else {
      console.log(item.name + "是一个文件");
    }
  });
});

// aaa是一个文件夹
// abc.txt是一个文件
// bbb是一个文件夹
// [
//   Dirent {
//     name: '1aa.txt',
//     parentPath: './axl/aaa',
//     path: './axl/aaa',
//     [Symbol(type)]: 1
//   }
// ]
// [
//   Dirent {
//     name: '1bb.txt',
//     parentPath: './axl/bbb',
//     path: './axl/bbb',
//     [Symbol(type)]: 1
//   }
// ]
```

试想一下，如果文件夹里面的层级很深，那么就要写很深的嵌套层级，那肯定是不好的，为了解决这个就要使用 [[03 函数的递归|函数的递归]] 来处理。

```js
import fs from "fs";

// 读取文件夹，获取文件信息
function readDirectory(path) {
  fs.readdir(path, { withFileTypes: true }, (err, files) => {
    files.forEach((item) => {
      if (item.isDirectory()) {
        readDirectory(`${path}/${item.name}`);
      } else {
        console.log(item.name + "是一个文件");
      }
    });
  });
}

readDirectory("./axl");
// abc.txt是一个文件
// 1aa.txt是一个文件
// 1bb.txt是一个文件

```

## 6.2 重命名

```js
import fs from 'fs'

// 文件夹重命名
fs.rename("./axl", "./abc", (err) => {
  if (err) {
    console.log("重命名失败")
  } else {
    console.log("重命名成功")
  }
})

// 文件重命名
fs.rename("./aaa.txt", "./ddd.txt", (err) => {
  if (err) {
    console.log("重命名失败")
  } else {
    console.log("重命名成功")
  }
})
```

# 七、常见命令英文缩写对照

| 指令    | 英文全称                    | 中文含义                       |
| ----- | ----------------------- | -------------------------- |
| mkdir | make directory          | 创建目录/文件夹                   |
| rmdir | remove directory        | 删除空目录/文件夹                  |
| rm    | remove                  | 删除文件或非空目录                  |
| cd    | change directory        | 切换工作目录                     |
| pwd   | print working directory | 显示当前工作目录路径                 |
| cp    | copy                    | 复制文件或目录                    |
| mv    | move                    | 移动/重命名文件或目录                |
| ls    | list                    | 列出当前目录下的文件和目录（Linux/macOS） |
| dir   | directory listing       | 列出当前目录下的文件和目录（Windows）     |
| cat   | concatenate             | 查看文件内容、合并文件                |

