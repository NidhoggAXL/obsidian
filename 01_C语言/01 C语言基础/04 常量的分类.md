### 字面常量
 ```c
 int main ()
	 {
		   3;==//字面常量==
			  return 0;
		}
```
###  修饰的常变量
#const 
```c
#include <stdio. h>
	int main()
	{
	const 常属性//const修饰的常变曩
	const int num = 4;
	printf("%d \n", num) ;
	num = 8;
	printf("%d \n", num) ;//字面赏曩==
	return 0;
```

###  define 定义的标识符常量枚举常量
```c
#define定义的标识符常量
	#define MAX 10
	int main()
	{
	int arr[MAX] = {0} ;
	printf(" %d\n",MAX) ;//输入结果为10
	return 0;
	}
```
###  枚举常量
* / /枚举——举例
	* 性别: 男; 女:. 保密
	* 三原色: 红、黄、蓝
	* 星期: 1, 2, 3, 4, 5, 6, 7
	* 枚举关键- enum
```c
enum Sex
	{
	MALE,//0
	FEMALE,//1
	SECRET//2
	};
	MALE,FEMALE,SECRET 一枚常量
```