# 本章重点
1. 数据类型详细介绍
2. 整形在内存中的存储:原码、反码、补码
3. 大小端字节序介绍及判断
4. 浮点型在内存中的存储解析

# 数据类型的介绍
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17052142130004a1mi8.png)



# 类型的基本归类
## 整型家族：
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1705214222000jcvuui.png)

## 浮点型家族
```c
float
double
```
## 结构类型
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17052142370002uwdec.png)


## 指针类型
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1705214249000s1fswm.png)


## 空类型![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17052142580004sak3w.png)



# 整型在内存中的存储
变量的创建是要在内存中开辟空间的，空间的大小是根据不同的类型而决定的。
```c
int a = 10;
```
我们知道 a 分配了四个字节，那么是如何存储的呢?

要知道这些 ，我们得先了解下面的概念：
## 补码、反码、补码
```c
计算机中的整型有符号数有三种表示方法，即原码、反码和补码三种表示万法均有符号位和数值位两部分，符号位都是用 0 表示“正”，用 1 表示"负”，而数值位三种表示方法各不相同。

当然无符号数在内存中也是用原码、反码、补码来表示的，但是三个码都相同。有符号数中的正数就是无符号数。

原码
直接按二进制按照正负数的形式翻译成二进制就可以。

反码
将原码的符号位不变，其他位依次按位取反就可以得到了

补码
反码 +1 就得到了补码
```

**对于整型来说：数据存放内存中其实存放的是补码。**

为什么呢?
```
在计算机系统中，整数一律用补码来表示和存储。原因在于，使用补码，可以将符号位和数值域统一处理，同时，加法和减法也可以统一处理(CPU只有加法器)此外，补码与原码相互转换，其运算过程是相同的，不需要额外的硬件电路，
```

![[Pasted image 20230906195601.png]]
可以看到对于a而言存储的都是补码。但是顺序有点**不对劲**。这是为什么呢？

## 大小端介绍
### 什么大端小端:
```
大端(存储)模式，是指数据的低位保存在内存的高地址中，而数据的高位，保存在内存的低地址中;

小端(存储)模式，是指数据的低位保存在内存的低地址中，而数据的高位保存在内存的高地址中

例子：
0*00 00 00 14-原码-14是低位-有右边到左边依次增大

下面存储地址从左向有右是有低到高的排列
大端存储的话就是 00 00 00 14
小端存储的话就是 14 00 00 00

```
**这里我们需要注意的是：我们这里讨论的是字节在内存中存放的顺序，不是二进制位存放的顺序。例如上面的 14 就是一个字节**。

### 为什么有大端和小端：
```
为什么会有大小端模式之分呢?这是因为在计算机系统中，我们是以字节为单位的，每个地址单元都对应着个字节，一个字节为8blt。但是在C语言中除了8bit的char之外，还有16bit的shor型，32bit的long型(要看具体的编译器]另外对于位数大于8位的处理器，例如16位或者32位的处理器，由于存器宽度大于一个字节，那么必然存在着一个如何将多个字节安排的问题，因此就导致了大端存储模式和小端存储模式

如一个16bit的short型x在内存中的地址为0x0010，x的值为0x1122那么0x11为高字节，0x22为低字节。对于大端模式，就将0x11放在低地址中，即0x0010中，0x22放在高地址中，即0x0011中。小瑞模式，刚好相反。我们常用的X86结构是小瑞模式，而KEL C51则为大端模式。很多的ARM，DSP都为小端模式。有些ARM处理器还可以由硬件来选择是大端模式还是小端模式
```


*百度2015年系统工程师笔试题*
```
清简述大端字节序和小端字节序的概念，设计一个小程序来判断当前机器的字节序
```
![[Pasted image 20230906203459.png]]

# char类型在内存中的存储
## char 和 unsigned char类型的范围
![[Pasted image 20230906214929.png]]

char 类型范围图![[Pasted image 20230906215150.png]]


## 练习：

1.![[Pasted image 20230906204751.png]]
```c
char 和是相同的 signed char 是相同的一种表示方法，只是我们在习惯char前面省略了 signed ，所以这两个在同一种的输入环境下结果是相同的。

解题思路：
a = -1 和 b = -1 都是一个整数，但是前面的定义类型是 char ，这里我们就要知道 我们只能去到一个字节-char 。

printf %d 打印十进制的有符号数字

 a = -1 和  b = -1在内存中是以补码方式存储的
10000000 00000000 00000000 00000001-原码
11111111 11111111 11111111 11111110-反码
11111111 11111111 11111111 11111111-补码
11111111-char类型可以读取的范围
这里的话要通过%d表示出来，就要进行整型提升了
11111111 11111111 11111111 11111111-整型提升-补码
10000000 00000000 00000000 00000001-原码 (-1)
结果就是char a = -1,signed char b = -1 打印的结果也是 -1

unsigned char c = -1
通过上面我们可以知道-1的补码是
11111111 11111111 11111111 11111111-补码
11111111-char类型可以读取的范围
00000000 00000000 00000000 11111111-整型提升(unsigned无符号所以补0)-补码
这里可以知道整型提升后 0 开头的是一个正数，原码、补码、反码相同
所以通过计算可以知道 结果是 255
所以cunsigned char c = -1 打印的结果是 255
```

 2.下面程序的输入结果是什么
 ```c
 1. 
 #include <stdio.h>
 int main()
 {
	 char a = -128;
	 printf("%u\n",a);
	 return 0;
 }

解题思路：
我们先要知道 %u 打印十进制的无符号数字

a = -128
10000000 00000000 00000000 10000000-原码
10000000 00000000 00000000 01111111-反码
11111111 11111111 11111111 10000000-补码
char a = -128
100000000-补码
11111111 11111111 11111111 11111111-补码-整型提升
但是我们要打印的是无符号数-就是正数，所以这里的原码、
反码、补码是相同的。
所以 打印的结果是 2^31+1 = 4294967168

2.
 #include <stdio.h>
 int main()
 {
	 char a = 128;
	 printf("%u\n",a);
	 return 0;
 }
char a = 128 中我们通过char类型的范围可以知道 char 中根本存放不了 128 ，但是换个思考方式 128 = 127 + 1 的话，就可以通过上面的char 环形图可以知道 char a = 128 和 cha a = -128 是相同的，所以打印的结果和 1 小题相同。

3.
int i = -20
unsigned int j = 10;
printf("%d\n",i+j);
//按照补码的形式进行运算，最后格式化成为有符号整数
unsigned int j = 10 就是无符号数，而这里的 int j = 10 是一个正数，也就是无符号数了，所以结果为 -10

4.
int main()
{
	unsigned int i;
	for(i = 9;i >= 0;i++)
	{
		printf("%u\n",i);
	}
	return 0;
}
这里刚开始是正常打印 9 8 7 6 5 4 3 2 1 0 但是在打印完 0 后 i-- 的结果就是 i = -1 而 i = -1 的二进制是
10000000 00000000 00000000 00000001-补码
11111111 11111111 11111111 11111110-反码
11111111 11111111 11111111 11111111-原码
int i 前面有一个 unsigned 无符号数 ，这里就把 -1 的原码当成一个整数来使用了 ， 整数原码、反码、补码都是相同的，这样下来 i 就变成了一个特别大的正数了。而且当运算到后面再次打印 0 的时候 i = -1就还会这样，这样就形成了一个死循环。

5.
int main()
{
	char a[1000];
	int i ;
	for (i = 0; i < 1000;i++)
	{
		a[i] = -1 -i;
	}
	printf("%d",strlen(a));
	retutn 0;
}
解题思路：
首先我们要知道 stelen 是计算字符串长度的 当遇到 \0 的时候停止计算

通过 for 循环，我们可以知道 a[1000] 中元素的范围大小是 -1 ~ -1000 ，但是我们这里的定义类型是 char ，而 char 的范围是 -128 ~ 127 总共 255 个元素。

这里如何把 -1 ~ -1000 和 char 类型的范围联系起来呢？
观察可以知道 -1 ~ -1000 是依次相减的 ，而观察 char 类型范围图（逆时针）也是依次相减的。
所以 a[1000] 中存储形式和 char 类型的范围是依次对应的，只是到 -128 的时候在 -1 是变成了 127 -char 范围中。
在使用 strlen 计算的时候就是到 char 类型范围完的时候刚好有一个 0 ('\0'是一个字符，它被 ASCII 赋值为 0 ，所以二者是等价的，要和0区分开)，所以结果为 255 .

6.
#include <stdio.h>
unsigned char i = 0;
int main()
{
	for ( i = 0; i <= 255 ; i++ )
	{
		printf("hello world\n");
	}
	return 0;
}
解题思路：
unsigned char 的范围是 0 ~ 255 。而 for 中的判断条件是 i <= 255 就恒成立，所以结果就是死循环。

```





# 浮点型在内存中的存储
常见的浮点数
```c
3.14159 1E10浮点数家族包括:foat.double.long double 类型。浮点数表示的范围: float.h 中定义

1E10 表示的是 1*10^10 的意思
```

浮点数存储的例子：
```c
int main()
{
	int n = 9;
	float *pFloat = (float *)&n;
	printf("n的值为：%d\n",n);
	printf("pFloat的值为：%f\n",*pFloat);

	*pFloat = 9.0;
	printf("n的值为：%d\n",n);
	printf("*pFloat的值为：%f\n",*pFloat);
	return 0;
}
```

^9d19ee

![[Pasted image 20230907204449.png]]

num和``*``pfloat在内存中明明是同-个数，为什么浮点数和整数的解读结果会差别这么大?要理解这个结果，一定要搞懂浮点数在计算机内部的表示方法


*详细解读*

根据国际标准IEEE(电气和电子工程协会)754任意一个二进制浮点数V可以表示成下面的形式:
```c
(-1)^S*M*2^E
(-1)^s表示符号位，当s=0，V为正数;当s=1,V为负数。
M表示有效数学，大于等于1，小于2.
2^E表示指数位

举例来说:十进制的 5.0 写成二进制是 101.0 相当 1.01 * 2 ^ 2 。那么按照上面V的格式可以得出 s = 0 . M = 1.01 ，E = 2
十进制的-5.0，写成二进制是-101.0，相当于 -1.01 * 2 ^ 2。那么，s = 1 ，M = 1.01 ，E = 2
```

32位浮点数：
![[Pasted image 20230907210023.png]]

64位浮点数：
![[Pasted image 20230907210121.png]]

* **IEEE 754 对有效数字M和指数E，还有一些特别规定**

```c
* 数字 M 的规定：
  前面说过，1<M<2也就是说，M可以写成 1.xxxxxx 的形式，其中xxxxxx表示小数部分
IEEE 754 规定，在计算机内部保存 M 时，默认这个数的第一位总是1，因此可以被舍去，只保存后面的xxxxxx部分比如保存 1.01 的时候，只保存 01 ，等到读取的时候，再把第一位的 1 加上去。这样做的目的，是节省 1 位有效数字.以32位浮点数为例，留给 M 只有23位，将第一位的1舍去以后，等于可以保存24位有效数字，这样可以增加精度。

* 至于指数E，情况就比较复杂
  首先，E为一个无符号整数 (unsigned int) 这意味着，如果 E 为 8 位，它的取值范围为 0-255;如果为 11 位它的取值范围为 0-2047。但是，我们知道，科学计数法中的E是可以出现负数的，所以IEEE754规定，存入内存时E的真实值必须再加上一个中间数，对于 8 位的 E ，这个中间数是 127 ;对于 11 位的 E ，这个中间数是 1023。比如， 210 的E是 10，所以保存成 32 位浮点数时，必须保存成 10+127=137，即 10001001。
```

然后，指数E从内存中取出还可以分为三种情况：

* **E不全为0或不全为1**
```c
这时，浮点数就采用下面的规则表示,即指数E的计算值减去127(或1023) 得到真实值,再将有效数字M加上第一位的 1，段如 0.5（1/2）二进制形式 0.1由于规定下数部分必须为 1 将小数点右移 1位，则为 1.0*2^(-1) ， 具价码为-1+127=126,麦示为 01111110 而尾数 1.0  去除整数部分为 0 ，补齐 0 到 23 位 00000000000000，训进二进制表示形式力
0 01111110 00000000000000
```

* **E全为0**
```c
这时，浮点数的指数E等于 1-127 (或者 1-1023)即为真实值，有效数字 M 不再加上第一位的1而，是还原为 0.xxxxxx 的小数。这样做是为了表示+-0，以及接近于0的很小的教学
```

* **E全为1**
```c
这时，如果有效数字 M全为0，表示 +- 无旁大(正负取决于符号位s)
```

*现在在对上面的题进行解释：*
![[深度剖析数据在内存中的存储#^9d19ee]]

```c
#include <stdio.h>
int main()
{
	int n = 9;
	//00000000 00000000 00000000 00001001-补码 
	float* pFloat = (float*)&n;
	printf("n的值为：%d\n", n);//打印结果就是为 0
	printf("pFloat的值为：%f\n", *pFloat);
	//0 00000000 00000000000000000001001-浮点数查看的话-E全为0的情况
	//（-1）^0*0.00000000000000000001001*2^-126
	//结果就是+-无穷接近0
	//%f 打印的是小数点后面的6位

	*pFloat = 9.0;
	//1001.0
	//1.001*2^3
	//(-1）^0*1.001*2^3
	//0 10000010 00100000000000000000000-补码
	printf("n的值为：%d\n", n);//1,091,567,616
	//最高位是 0 就是正数，正数的原码、补码、反码相同
	//01000001000100000000000000000000-原码

	printf("*pFloat的值为：%f\n", *pFloat);//9.0
	return 0;
}
```

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1705214282000sptuiy.png)


![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1705214298000sdq33v.png)

