# 重点
* 结构体
	* 结构体类型的声明结构的自引用
	* 结构体变量的定义和初始化。
	* 结构体内存对齐
	* 结构体传参
	* 结构体实现位段（位段的填充&可移植性)·
* 枚举
	* 枚举类型的定义
	* 枚举的优点
	* 枚举的使用
* 联合
	* 联合类型的定义。
	* 联合的特点
	* 联合大小的计算

# 结构体
## 结构体的声明
> [!tip] 结构体的基础知识
> 结构是一些值的集合，这些值称为成员变量。"结构的每个成员可以是不同类型的变量

> [!tip] 定义
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708409253000n7dnxd.png)
### 特殊声明
比如：
1. 匿名结构体类型
```C
struct
{
	int a;
	char b;
	flosat c;
}x;

struct
{
	int a;
	char b;
	float c;
}a[20],*p;
```
都省略了结构标签（tag）
同时 上面两个虽然成员列表相同，但是他的结构体类型是不同的。例如：
```C
p=&x;
//这个就是错误的。
```

## 结构体的自引用
```C
//错误的写法，不可以自己套用自己，注意不是递归
//如果可以，那么sizeof(strcut Node)是多少呢？
//这个没有办法计算的
struct Node
{
	int date;
	struct Node n;
};

sypedef struct 
{
	int date;
	Node* next;
}Node;
//代码是从上向下运行的，当使用Node的时候，Node还没有定义出来。

//正确的写法
struct Node
{
	int date;
	struct Node* next;//存放下一个结构体的地址，指针大小是可以计算的
};
```

## 结构体变量的定义和初始化
### 结构体变量的定义
```C
struct Stu
{
	int x;
	int y;
}p1;//声明类型的同时定义变量p1
struct Stu p2;//定义结构体变量p2
```

### 结构体变量的初始化
```C
#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

struct T
{
	int weigth;
	short age;
};

struct N
{
	struct T t;//结构体的嵌套
	char b;
	char arr[10];
};

int main()
{
	struct N n = { {55.6,20},'A',"hello"};//struct N n = {0};
	//每个结构体初始化时，都要加大括号{}。
	printf("%c %s\n", n.b, n.arr);
	printf("%d\n", n.t.weigth);
	return 0;
}
```

## 结构体内存对齐
> [!tip] 一个问题
> 如何计算结构体的内存呢？

先看一段代码，计算内存占了多少？
```C
struct S
{
	char c1;
	int a;
	char c2;
};
struct T
{
	char c1;
	char c2;
	int a;
}
int main()
{
	struct S s = {0};
	struct T t = {0};
	printf("%d\n",sizeof(s));
	printf("%d\n",sizeof(t));
	return 0;
}
```
打印的结果时12和8，这到底是为什么呢？

> [!tip] 考点 如何计算？
> 首先得掌握结构体的对齐规则：
> 1. 第一个成员在与结构体变量偏移量为0的地址处。
> 2. 其他成员变量要对齐到某个数字(对齐数）的整数倍的地址处。
> 3. 结构体总大小为最大对齐数(每个成员变量都有一个对齐数）的整数倍
> 4. 如果嵌套了结构体的情况，嵌套的结构体对齐到自已的最大对齐数的整数倍处，结构体的整体大小就是所有最大对齐数(含嵌套结构体的对齐数
>> [!tip] 注解：
>  对齐数=编译器默认的一个对齐数与该成员大小的较小值。
>  VS中以的自为8，有些编译器是没有对齐数的。这时候成员大小就是对齐数。比如gcc就是没有对齐数。

![[自定义数据类型的存储空间示意图]]

> [!tip] 那为什么要存在内存对齐呢？
> 1. 平台原因(移植原因)︰不是所有的硬件平台都能访问任意地址上的任意数据的;某些硬件平台只能在某些地址处取某些特定类型的数据，否则抛出硬件异常。
> 2. 性能原因:数据结构(尤其是栈)应该尽可能地在自然边界上对齐。原因在于，为了访问未对齐的内存，处理器需要作两次内存访问;而对齐的内存访问仅需要一次访问。
> > [!tip] 总体来说
> > 结构体的内存对齐是拿**空间**来换取**时间**的做法。

那么在设计结构体的时候，我们既要满足对齐，又要节省空间，如何做到：
>让占用空间小的成员尽量集中在一起

```C
struct S1
{
	char c1;
	int i;
	char c2;
};

strctt S1
{
	char c1;
	char c2;
	int i;
};

这两个代码，其中S2比S1占用的空间小
```

那我们知道代码运行的时候，是从上到下的运行的，那么代码的编写会不会改变存储的空间大小呢？
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17085038820002a8uto.png)
上面可以看出是不会改变的。

## 修改默认对齐数
使用一种语法形式
```C
#pragma pack(对齐数)
中间代码块
#pragma pack()//取消默认对齐数
```


## 结构体传参
结构体进行传参数的时候，可以传地址也可以传结构体，那么这两种，哪一种更好呢？
```C
struct S
{
	int a;
	double d;
};

//都是进行打印
void print1(struct S tmp)
void print2(const struct S* tmp)//保证结构体不会被改变

int main()
{
	print1(s);//传结构体
	print2(&s);//传地址
	return 0;
}
```
这里先要知道，传递结构体的时候，是对结构体的一份临时拷贝，根据结构体的大小进行拷贝。当传递一个地址的时候，是一个指针来进行接收。
> [!tip] 首选print2函数。原因：
> 函数传参的时候,参数是需要压栈,会有时间和空间上的系统开销。
如果传递一个结构体对象的时候，结构体过大，参数压栈的的系统开销比较大，所以会导致性能的下降。

# 位段
结构体实现位段的能力
## 什么是位段
> [!tip] 位段的声明和结构是类似的，又两个不同：
> 1. 位段的成员必须是int, unsigned int 或signed int 。
> 2. 位段的成员名后边有一个冒号和一个数字。

比如：
```C
struct A
{
	int a:2;
	int b:5;
	int c:10;
	int d:30;
};
```
A就是一个位段类型/
那么段A的大小是多少呢？
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708507208000lhndzc.png)
结果是8，那为什么是8呢？
首先要知道后面的那个2、5、10、30是什么意思。位段里面这里的数字其实表示的是bit位，这样表示a只给2个bit位、b给5个bit位……。这种算下来，这个结构体总共是47个bit位，总共是6个字节(一个字节是8个bit位-``6*8=48``)，就算是这样也还是没有8个字节啊，那又是为什么呢？

## 位段的内存分布
> 1. 位段的成员可以是int unsigned int signed int或者是char(属于整形家族）类型
> 2. 位段的空间上是按照需要以4个字节(int ）或者1个字节(char)的方式来开辟的。
> 3. 位段涉及很多不确定因素，位段是不跨平台的，注重可移植的程序应该避免使用位段。

![[位段内存分布示意图]]
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708511316000uhpcwe.png)

## 位段的跨平台问题
> 1. int位瘟被当成有符号数还是无符号数是不确定的。
> 2. 位段中最大位的数目不能确定。(16位机器最大16，32位机器最大32，写成27，在16位机器会出问题。
> 3. 位段中的成员在内存中从左向右分配，还是从右向左分配标准尚未定义。
> 4. 当一个结构包含两个位段，第二个位段成员比较大，无法容纳于第一个位段剩余的位时，是舍弃剩余的位还是利用，这是不确定的。
> >[!tip] 总结
> >跟结构相比，位段可以达到同样的效果，但是可以很好的节省空间，但是有跨平台的问题存在。

## 位段的应用
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708511911000r0g3t3.png)
在计算机网络的传输信息的时候，这时候不仅仅是传输你要发出的那个数据，而是一个数据包。这里面有很多数据的类型是相同的，这时候使用位段的来分包数据，就是一个非常节省空间的方法。

# 枚举
枚举顾名思义就是一一列举
把可能的取值一一列举
比如我们生活中的：
> 一周的星期一到是期曰是有限的7天,可以—列举。性别有:男、女、保密，也可以——列举。
月份有12个月，也可以——列举

这里就可以使用枚举了。

## 枚举类型的定义
```C
enum Sex//类型
{
	//枚举的可能取值-枚举常量
	MALE,//男
	FEMALE,//女
	SECRET//保密,注意这里就没有英文的逗号了
};

int main()
{
	enum Day s = MALE;//对枚举进行赋值，只能用枚举类型里面的进行赋值
	return 0;
}
```
枚举类型是一个枚举常量，那么是常量的话，那可以打印出来。
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17085128630004hf04j.png)

这里可以看到，枚举常量从上到下依次排序。那么是否可以改变次序呢？

从下面代码看是可以的。
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708512993000z2robt.png)

**这里的2、4、8并不是一个改变的操作，因为常量是不应许改变的，这其实一个赋值的操作，赋值了常量的一个初始值。**

那赋值的变化也会引起排序的改变吗？
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708513501000sffg5q.png)

这样看是会的，但是从上到下，如果改变了，那么后面的也会发生改变。


## 枚举大小的计算
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708522404000e88mut.png)

上面可以看出，是4个字节。因为枚举在进行初始化的时候就是通过整型来进行排列的。


## 枚举的优点
我们可以使用#define 定义常量，为什么非要使用枚举？
> [!tip] 枚举的优点
> 1. 增加代码的可读性和可维护性
> 2. 和#define定义的标识符比较枚举有类型检查，更加严谨。
> 3. 防止了命名污染(封装)
> 4. 便于调试
> 5. 使用方便，一次可以定义多个常量

## 枚举的使用
```C

enum Sex//类型
{
	//枚举的可能取值-枚举常量
	MALE ,//男
	FEMALE ,//女
	SECRET = 0 //保密,注意这里就没有英文的逗号了
};

int main()
{
	enum Day s = MALE;//对枚举进行赋值，只能用枚举类型里面的进行赋值
	s = 10;
	return 0;
}

```

# 联合(共用体)
## 联合类型的定义
> 联合也是一种特殊的自定义类型这种类型定义的变量也包含一系列的成员，特征是这些成员公用同一块空间（所以联合也叫共用体）。比如:

```C
//联合类型的说明
union Un
{
	char c;
	int i;
};
```

那使用时的情况是什么呢？
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708520254000sor57j.png)

## 联合的特点
> 联合的成员是共用同一块内存空间的，这样一个联合变量的大小，至少是最大成员的大小(因为联合至少得有能力保存最大的那个成员)。

## 面试题
> 判断当前计算机的大小端存储
[[深度剖析数据在内存中的存储#大小端介绍]]
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1708521560000hrrklj.png)

```C
int mian()
{
	int a=1;
	if(1==*(char*)&a)
	{
		printf("小端存储\n");
	}
	else
	{
		printf("大端存储\n");
	}
	return 0;
}
```

使用联合体来实现：
```C
int Cheak_sys()
{
	union Un
	{
		char c;
		int i;
	}u;
	u.i = 1;
	return u.c;
}
int main()
{
	int ret;
	ret=Cheak_sys();
	if (ret == 1)
	{
		printf("小端存储\n");
	}
	else
	{
		printf("大端存储\n");
	}
	return 0;
}
```

## 联合大小的计算
> * 联合的大小至少是最大成员的大小。
> * 当最大成员大小不是最大对齐数的整数倍的时候，就要对齐到最大对齐数的整数倍。

```C
union Un
{
	int i;//对齐数4
	char arr[5];//对齐数1，
	//注意这里不是5，对齐数是数据的大小，不是总的大小
	//类似与 编写了5个 char 类型。
	//vs2013的最大对齐数是8。int和char的对齐数都不是最大对齐数的整倍。
};

int main()
{
	union Un n;
	printf("%d\n", sizeof(n));
	//对齐到最大对齐数8的整数倍，就是8
	return 0;
}
```




