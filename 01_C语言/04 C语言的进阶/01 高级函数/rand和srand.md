1. tand函数时随机生成一个随机数，这个数字的范围时0~32767.
2. 代码：
```c
#include <stdlib.h>
int ret = 0;
srand(整型数字);
ret = rand();
//这种得出的随机值具有一定的规律。当这个整型数字是一个随机的变量值的时候才是没有规律的随机值。
```
3. 问题：
	* 有一个bug，要生成随机数但是要我们还输入一个随机数。
	* 这里输入的随机数就要用到[[时间戳]]来进行随机值的输入。
