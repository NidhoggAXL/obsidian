# 什么是文件(file)
磁盘上的文件是文件。
但是在程序设计中有：程序文件、数据文件。

**程序文件
> 包括源程序文件（后缀为.c ) ,目标文件 ( windows环境后缀为.obj ) ,可执行程序 ( windows环境后缀为.exe )。

**数据文件：**
> 文件的内容不一定是程序，而是程序运行时读写的数据，比如程序运行需要从中读取数据的文件，或者输出内容的文件。

# 文件名、
一个文件要有一个唯一的文件标识，以便用户识别和引用。
文件名包含3部分：文件路径+文件名主干+文件后缀
``例如:c:\code\test.txt``
为了方便起见，文件表示常被称为**文件名**。

# 文件类型
根据数据的组织形式，数据文件被称为**文本文件**或者**二进制文件**。
数据在内存中以二进制的形式存储，如果不加转换的输出到外存，就是**二进制文件**。
如果要求在外存上以ASCII码的形式存储，则需要在存储前转换。以ASCII字符的形式存储的文件就是**文本文件**。

**—个数据在内存中是怎么存储的呢?**
字符一律以ASCII形式存储，数值型数据既可以用ASCIIl形式存储，也可以使用二进制形式存储。
如有整数10000，如果以ASClI码的形式输出到磁盘，则磁盘中占用5个字节(每个字符一个字节)，而二进制形式输出，则在磁盘上只占4个字节(VS2013测试）。
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709130351000uzm8at.png)


# 文件缓冲区
ANSIC标准采用""缓冲文件系统"处理的数据文件的，所谓缓冲文件系统是指系统自动地在内存中为程序中每一个正在使用的文件开辟一块′文件缓冲区"。从内存向磁盘输出数据会先送到内存中的缓冲区，装满缓冲区后才一起送到磁盘上。如果从磁盘向计算机读入数据，则从磁盘文件中读取数据输入到内存缓冲区（充满缓冲区），然后再从缓冲区逐个地将数据送到程序数据区（程序变量等)。缓冲区的大小根据C编译系统决定的。
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17091846070002cqxrc.png)



# 文件指针
缓冲文件系统中，关键的概念是“**文件类型指针**”,简称“**文件指针**”。
每个被使用的文件都在内存中开辟了一个相应的文件信息区，用来存放文件的相关信息(如文件的名字，文件状态及文件当前的位置等)。这些信息是保存在一个结构体变量中的。该结构体类型是有系统声明的，取名**FILE**.
例如,VS2008编译环境提供的 stdio.h 头女件中有以下的文件类型申明：
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17093597540001fywg6.png)

不同的C编译器的FILE类型包含的内容不完全相同，但是大同小异。
每当打开一个文件的时候，系统会根据文件的情况自动创建一个FlLE结构的变量，并填充其中的信息，使用者不必关心细节。
一般都是通过一个FILE的指针来维护这个FILE结构的变量，这样使用起来更加方便。
卜面我们可以创建—个FILE*的指针变量:
```C
FILE* pf;//文件指针变量
```
定义pf是一个指向FILE类型数据的指针变量。可以使pf指向某个文件的文件信息区（是一个结构体变量)。通过该文件信息区中的信息就能够访问该文件。也就是说，**通过文件指针变量能够找到与它关联的文件**。
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17093601140000j2pcb.png)


# 文件的打开和关闭
文件在读写之前应该先**打开文件**，值使用接收之后应该**关闭文件**。
在编写程序的时候，在打开文件的同时，都会返回一个FILE*的指针变量指向该文件，也相当于建立了指针和文件的关系。
ANSIC规定使用fopen函数来打开文件，fclose来关闭文件。
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17093603380009qfcyx.png)
打开方式：
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709360393000akvajl.png)

**打开文件的路径有：**
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709360918000er8uau.png)

```C
#include <stdio.h>
#include <errno.h>
#include <string.h>

int main()
{
	FILE* pf = fopen("text.txt", "w");
	//打开文件
	if (pf == NULL)
	{
		printf("%s\n", strerror(errno));
		return 0;
	}
	//读文件
	//关闭文件
	fclose(pf);
	pf = NULL;
	return 0;
}
```

# 文件的顺序读写
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/170936144400086zted.png)
[[通讯录(contact)]]
## 设备输入和输出-单个字符
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709362046000w5a3i9.png)
```C
int main()
{
	in ch = fgetc(stdin);
	fputc(ch,stdout);
	return 0;
}
```

## 设备输入和输入-单行
### fgets(读取一行) puts(输入一行)
#### fgets
> [!tip] 定义
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17093651710004i91wq.png)
> string 读取内容存放的地方
> n 是最大读取的个数
> stream 读取的文件


**读取一行：**
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17093649450000pdfxy.png)
**其中puts在打印的时候自带换行**。

### fputs(写入一行)
> [!tip] 定义
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709365303000rys6jo.png)
> string 是写入的字符串
> stream 是写入的地方


**写入一行：**
```C
#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <errno.h>
#include <string.h>

int main()
{
	char arr[1024] = { 0 };
	FILE* pf = fopen("text.txt", "w");
	//打开文件
	if (pf == NULL)
	{
		printf("%s\n", strerror(errno));
		return 0;
	}
	//写入
	fputs("hello", pf);
	fputs("monty", pf);
	//关闭文件
	fclose(pf);
	pf = NULL;
	return 0;
}

```
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709365502000zqcfld.png)
**fputs 可以看出写入时没有换行的，如果要换行要自己输入**

### 从键盘输入，屏幕输出
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709365901000f17zvi.png)



### fprintf(格式化写文件) fscanf(格式化输入数据)
> [!tip] 定义
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709366614000agjxqp.png)
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17093666620009xqtyb.png)
> **fprintf 比 printf 多了一个stream**
> stream 写入的地方
> format 写入的数据


![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709367317000t19dc4.png)

> [!tip] 定于
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709367372000do6xyw.png)
> 比起scanf多了个stream（位置）

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17093675170007wqmev.png)

#### 一起使用fcanf和fprintf
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709367878000ohllum.png)





### sprintf和sscanf
> spritnf 把格式化的数据转换成字符串
> sscanf 读取格式化的数据到外面
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709370696000fwlmbp.png)


### fread和fwrite(二进制输入输出)
> 定义：
> **fwrite:**
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709448269000o6o1le.png)
> f**read:**
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709448605000inqct4.png)
> buffer 指向写入文件内容的指针
> size   元素的大小，单位字节
> count  写入元素的最大个数
> > 返回值size_t 是真实的返回值：
> > 如果返回的1，则读了一个，返回2，表示读了2个。
> stream 写入的文件的指针

fwrite:
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709448990000yb6atq.png)

fread:
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709449165000d2gs1n.png)


# 对比一组函数
scanf/printf
> 是针对标准输入流/标准输出流的格式化输入/输出语句


fscanf/fprintf
>  是针对所有输入流/所有输出流的格式化输入/输出语句


sscanf/sprintf
> sscanf是从字符串中读取格式化的数据
sprintf是把格式化数据输出成(存储到)字符串


# 文件的随机读写
## fseek
> 根据文件指针的位置和偏移量来定位文件指针。
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709598844000qbx6v4.png)
> offset 偏移量-单位字节
> origin 文件指针的当前位置
> 	SEEK_CUR 文件指针的当前位置
> 	SEEK_END 文件的末尾
> 	SEEK_SET 文件的起始位置

文件内容：
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709599884000eawiif.png)

代码：
```C
#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main()
{
	FILE* pf = fopen("test.txt", "r");
	if (pf == NULL)
	{
		return 0;
	}
	//定位文件指针-读取文件
	//读取文件的时候要注意是否越界访问
	fseek(pf, 4, SEEK_CUR);//当前位置
	int ch = fgetc(pf);
	printf("SEEK_CUR:%c\n",ch);

	fseek(pf, 0, SEEK_SET);//起始位置
	int ph = fgetc(pf);
	printf("SEEK_SET:%c\n",ph);

	fseek(pf, -2, SEEK_END);//末尾位置
	int ah = fgetc(pf);
	printf("SEEK_END:%c\n", ah);
	
	//越界访问是一个随机值
	fseek(pf, 2, SEEK_END);//末尾位置
	int dh = fgetc(pf);
	printf("SEEK_END:%c\n", dh);
	//关闭文件
	fclose(pf);
	pf = NULL;
	return 0;
}
```

结果：
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709599934000r71s4v.png)

### offsetof(偏移量)
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/171016534800083eklm.png)

**模拟自主实现：**
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1710166074000hf54lh.png)
```
因为不知道struct的起始地址，所以我们就把他转为0地址
((struct S*)0)

知道了地址后，就可以找到元素
(((struct S*)0)-> mem_name)

但是我们要的是地址，偏移量是对首元素的偏移
&(((struct S*)0)-> mem_name)

最后我们是要打印偏移的大小%d,所以强制类型转换为int
(int)&(((struct S*)0)-> mem_name)
```



## ftell
> 返回文件指针相对于起始位置的偏移量
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709600483000n74vlu.png)

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17096008460006l5pk3.png)

## rewind
> 让文件指针的位置回到文件的起始位置
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709600977000wrful9.png)

![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/17096012760003e4swj.png)



# 文件结束的判定feof
**被错误使用的 feof**
> EOF-end of file - 文件结束标志
> ![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709617659000cps25o.png)
> 牢记︰在文件读取过程中，不能用feof函数的返回值直接用来判断文件的是否结束。
> 而是**应用于当文件读取结束的时候，判断是读取失败结束，还是遇到文件尾结束**。
> 1. 文本文件读取是否结束，判断返回值是否为EOF ( fgetc )，或者NULL ( fgets )例如︰
> * fgetc判断是否为EOF.
> * fgets判断返回值是否为NULL.
> 2.  二进制文件的读取结束判断，判断返回值是否小于实际要读的个数。


文件结束的标志，**当文件为空时**就只有文件结束标志：
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709617927000r2tcrv.png)

[[函数介绍#strerroe和errno和perror]]
**文本文件的正确使用例子：**
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709619897000sruugr.png)

```C
#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	FILE* pf = fopen("test.txt", "r");
	if (pf == NULL)
	{
		perror("open file text.txt");
	}
	int ch;
	while ((ch = getc(pf)) != EOF)
	{
		putchar(ch);
	}
	if (ferror(pf))//ferror 读取错误结束，则返回一个非0值
	{
		printf("error\n");
	}
	else if (feof(pf))
	{
		printf("\n读取的结束标志\n");
	}
	fclose(pf);
	pf = NULL;
	return 0;
}
```
![gh](https://raw.githubusercontent.com/AXLflechazoPN/Obsidian/main/2024/1709620742000cdq5a9.png)
