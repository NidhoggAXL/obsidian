指向[函数指针数组]的指针
```C
int arr[10]={};
int (*p)[10]=&arr;取出数组的地址

int (*pf)(int,int);函数指针
int (*pfArr[4])(int,int);pfArr是一个数组-函数指针的数组
int (*(*ppfArr)[4])(int,int)=&pfArr;
ppfArr 是一个数组指针，指针指向的数组有四个元素
指向的数据的每个元素的类型是一个函数指针 int(*)(int,int)
```



