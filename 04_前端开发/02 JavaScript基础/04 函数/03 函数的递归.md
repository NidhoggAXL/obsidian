函数调用自己还有一个专业的名词，叫做递归(Recursion)

递归是一种重要的编程思想:

* 将一个复杂的任务，转化成可以重复执行的相同任务

练习：次方的递归编写

```js
function pow(x, n) {
  if (n === 1) return x
  return x * pow(x, n-1)
}

console.log(pow(2,3))//8
```








