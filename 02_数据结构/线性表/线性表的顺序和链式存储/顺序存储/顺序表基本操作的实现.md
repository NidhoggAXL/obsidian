### 函数结果状态代码
```c
#define TRUE  1//符合事实的
#define FALSE 0//假的
#define OK    1
#define ERROR 0//错误
#define INFEASIBLE -1////不可行的
#define OVERFLOW    -2//溢出
```

### Status 是函数的类型，其值是函数结果状态代码
```c
typedef int Status;
typedef char ElemType;
```

### **补充操作：**
销毁线性表L（C++）
```c
//destroy-破坏；销毁
void DestroyList(SqList &L)
{
	if(L.elem) delete L.elem;//释放存储空间
}
```

清空线性表L
```c
void ClearList(SqList &l)
{
	L.ength = 0;
}
```

求线性表L的长度
```c
//getlength-获取长度
int GetLength(SqList L)
{
	return (L.length);
}
```

判断线性表L是否为空
```c
//isrmptyl-为空；否为空
int IsEmpty(SqList L)
{
	if(L.length==0)return 1;
	else return 0;
}
```


### **理论操作**
* 线性表L的初始化（参数用引用）![[Pasted image 20231111205641.png]]
* 顺序表的取值![[Pasted image 20231111205336.png]]
* 线性表的查找![[Pasted image 20231111210100.png]]![[Pasted image 20231111210538.png]]
	* 顺序表的查找算法分析：![[Pasted image 20231111212920.png]]![[Pasted image 20231111212958.png]]
* 顺序表的插入![[Pasted image 20231111214011.png]]![[Pasted image 20231111214636.png]]
	* 顺序表的插入算法分析![[Pasted image 20231111215615.png]]![[顺序表的插入算法分析]]
* 线性表的删除![[Pasted image 20231112154538.png]]![[Pasted image 20231112154823.png]]
	* 线性表插入算法分析![[Pasted image 20231112155158.png]]

# 顺序表的总结
## 顺序表的操作算法分析
* 时间复杂度
	* 查找、插入、删除算法的平均时间复杂度为O(n)
* 空间复杂度
	* 显然，顺序表操作算法的空间复杂度S(n)=O(1)  (没有占用辅助空间)

## 顺序表优缺点
![[Pasted image 20231112155656.png]]