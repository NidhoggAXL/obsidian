# 1 系统分析
## 1.1 背景分析
考试报名系统是一个学校不可缺少的部分，它对于学校的管理者和学生来说都至关重要，所以一个良好的考试报名系统应该能够为用户提供充足的信息和功能。考试报名系统对于学校加强考试管理有极其重要的作用。随着学生数量和考试数量的日益庞大，如何管理如此庞大的数据显得极为复杂，传统的手工管理工作量大且容易出错。

随着计算机科学技术的不断成熟，使用计算机对考试报名系统进行管理，具有手工管理所无法比拟的优势。这些优点能够极大地提高学校和学生的效率，也是学校走向信息化、科学化、国际化的重要条件。因此，开发一套考试报名系统具有十分重要的意义。

## 1.2 功能分析
一个简单的考试报名系统，首先应该要可以显示学生的报名情况，可以随时观察报名的进度和情况。其次，考试报名系统还应该具有插入、删除、修改功能，以保证同学可以随时更改自己的考试报名情况。最后，考试报名系统软件还应该确保软件可以正常关闭。

综上所述，一个简单的报名系统应该包括：输入、输出、插入、删除、修改、终止（退出）系统的功能

# 2 设计
## 2.1 数据结构设计
如上功能分析所述，该系统要求大量的增加、删除、修改操作，可能会存在内存不够的情况，而带哨兵位的双向循环链表进行增加、删除等操作十分简便，因此考虑使用带哨兵位的双向循链表数据结构。
![[考试报名系统]]

## 2.2 类结构的设计
链表结点类（LTNode）
```C
#define MAX 20
typedef struct ListNode
{
	char name[MAX];//姓名
	char id[MAX];//学号
	char sex[MAX];//性别
	char age[MAX];//年龄
	char subject[MAX];//报考类别
	struct ListNode* next;//结构体的尾指针
	struct ListNode* prev;//结构体的头指针
}LTNode;
```

## 2.3 成员与操作设计
### 2.3.1 链表节点类
* string id  
* string sex
* string name
* string  age
* string subject
* ListNode* next  结构体的尾指针
* struct ListNode* prev  结构体的头指针

### 2.3.2 链表的操作类
```C
LTNode* ListInit();//哨兵位头节点的创建
LTNode* BuyListNode(LTNode* phead);//创建一个新的结构体
void ListDestory(LTNode* phead);//销毁数据
void ListPrint(LTNode* phead);//打印
void ListPushFront(LTNode* phead);//头插入
void ListPushBack(LTNode* phead);//尾插入
void ListPopFront(LTNode* phead);//头删除
void ListPopBack(LTNode* phead);//尾删除
LTNode* ListFind(LTNode* phead);//查找

//利用返回值来进行操作
void ListInset(LTNode* pos);//在pos前面位置插入
void ListBack(LTNode* pos);//在pos前面位置插入
void ListErase(LTNode* pos);//删除pos位置的元素
```

# 3 实现
## 3.1 头尾插入功能的实现
### 3.1.1 头插入功能的实现
```C
void ListPushFront(LTNode* phead)//头插入
{
	assert(phead);
	LTNode* newnode = (LTNode*)malloc(sizeof(LTNode));
	assert(newnode);
	newnode->next = NULL;
	newnode->prev = NULL;
	printf("请依次输入考生的考号、姓名、性别、年龄及报考类别!\n");
	scanf("%s %s %s %s %s\n", &(newnode->id),&(newnode->name),&(newnode->sex),&(newnode->age),&(newnode->subject));
	
	LTNode* next = phead->next;
	phead->next = newnode;
	newnode->prev = phead;

	newnode->next = next;
	next->prev = newnode;
}
```

### 3.1.2 尾插入功能的实现
```C
void ListPushBack(LTNode* phead)//尾插入
{
	assert(phead);
	LTNode* newnode = (LTNode*)malloc(sizeof(LTNode));
	assert(newnode);
	newnode->next = NULL;
	newnode->prev = NULL;
	printf("请依次输入考生的考号、姓名、性别、年龄及报考类别!\n");
	scanf("%s %s %s %s %s", &(newnode->id), &(newnode->name), &(newnode->sex), &(newnode->age), &(newnode->subject));
	LTNode* tail = phead->prev;
	tail->next = newnode;
	newnode->prev = tail;

	phead->prev = newnode;
	newnode->next = phead;
}
```


## 3.2 头尾删除功能的实现
### 3.2.1 头删除功能的实现
```C
void ListPopFront(LTNode* phead)//头删除
{
	assert(phead && phead->next != phead);
	LTNode* next = phead->next;
	LTNode* nextNext = next->next;
	phead->next = nextNext;
	nextNext->prev = phead;
	free(next);
	next = NULL;
}
```

### 3.2.2 尾删除功能的实现
```C
void ListPopBack(LTNode* phead)//尾删除
{
	assert(phead);
	LTNode* tail = phead->prev;
	LTNode* prev = tail->prev;
	phead->prev = prev;
	prev->next = phead;
	free(tail);
	tail = NULL;
}
```

## 3.3 查找功能的实现
### 3.3.1 查找并打印查找的数据
```C

LTNode* ListFind(LTNode* phead)//查找
{
	assert(phead);
	LTNode* cur = phead->next;
	char input[IMAX];
	printf("\n请输入要查找的报名学生学号：");
	scanf("%s", &input);
	getchar();
	while (cur!= phead)
	{
		if (strcmp(cur->id,input ) == 0)
		{
			printf("\n考号：%s 姓名：%s 性别:%s 年龄:%s 报考类别:%s  \n", cur->id, cur->name, cur->sex, cur->age, cur->subject);
			return cur;
		}
		cur = cur->next;
	}
	printf("没有该考生！");
	return NULL;
}
```

### 3.3.2 查找并改变数据
```C
void ListChange(LTNode* pos)//确认是否改变数据
{
	LTNode* cur = pos;
	int input;
	printf("\n====请确定是否改变数据：1. change  0.exit ====\n");
	scanf("%d", &input);
	if (input)
	{
		LTNode* next = cur->next;//pos后的结构体
		LTNode* prev = cur->prev;//pos前的结构体
		LTNode* newnode= (LTNode*)malloc(sizeof(LTNode));
		printf("\n请依次输入考生的考号、姓名、性别、年龄及报考类别!\n");
		scanf("%s %s %s %s %s", &(newnode->id), &(newnode->name), &(newnode->sex), &(newnode->age), &(newnode->subject));
		prev->next = newnode;
		newnode->prev = prev;

		next->prev = newnode;
		newnode->next = next;
		free(cur);
		//printf("\n考号：%s 姓名：%s 性别:%s 年龄:%d 报考类别:%s  \n", newnode->id, newnode->name, newnode->sex, newnode->age, newnode->subject);
	}
}
```

## 3.4. 菜单栏
### 3.4.1 菜单栏的选择
```C
void menu()//确定进行的操作
{
	printf("\n***********欢迎进入考试查询系统*************\n");
	printf("****************操作列表********************\n");
	printf("***********1. 首行进行报名******************\n");
	printf("***********2. 尾行进行报名******************\n");
	printf("***********3. 删除首行报名学生数据**********\n");
	printf("***********4. 删除尾行报名学生数据**********\n");
	printf("***********5. 查询报名学生数据**************\n");
	printf("***********6. 更改报名学生数据**************\n");
	printf("***********7. 查询所有报名学生数据**********\n");
	printf("***********0. 退出考试查询系统**************\n");
}
```

### 3.4.2 功能选择
```C
int main()
{
	LTNode* plist = ListInit();//哨兵位头节点的创建
	LTNode* pos;
	int input = 0;
	do
	{
		menu();
		printf("请确认操纵:");
		scanf("%d", &input);
		switch (input)
		{
		case 1:
			ListPushFront(plist);//头插入
			break;
		case 2:
			ListPushBack(plist);//尾插入
			break;
		case 3:
			ListPopFront(plist);//头删除
			break;
		case 4:
			ListPopBack(plist);//尾删
			break;
		case 5:
			ListFind(plist);//查找
			break;
		case 6:
			pos = ListFind(plist);
			ListChange(pos);//改变数据
			break;
		case 7:
			ListPrint(plist);//打印
			break;
		case 0:
			printf("退出考试查询系统！\n");
			break;
		default:
			printf("输入错误，请重新输入\n");
			break;
		}
	} while (input);
	return 0;
}
```

# 4.总模块源代码
## 4.1 System
```C
#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <malloc.h>
#include <stdbool.h>
#include <assert.h>

#define MAX 10
#define IMAX 4


typedef struct ListNode
{
	char name[MAX];//姓名
	char id[IMAX];//学号
	char sex[MAX];//性别
	char age[MAX];//年龄
	char subject[MAX];//报考类别
	struct ListNode* next;//结构体的尾
	struct ListNode* prev;//结构体的头
}LTNode;


void menu();//确定进行的操作


LTNode* ListInit();//哨兵位头节点的创建
void ListPrint(LTNode* phead);//打印
void ListPushFront(LTNode* phead);//头插入
void ListPushBack(LTNode* phead);//尾插入

void ListPopFront(LTNode* phead);//头删除
void ListPopBack(LTNode* phead);//尾删除

LTNode* ListFind(LTNode* phead);//查找
void ListChange(LTNode* pos);//确认是否改变数据
```


## 4.2 System
```C
#define _CRT_SECURE_NO_WARNINGS 1
#include "System.h"

LTNode* ListInit()//哨兵位头节点的创建
{
	LTNode* phead = (LTNode*)malloc(sizeof(LTNode));
	assert(phead);
	phead->next = phead;
	phead->prev = phead;
	return phead;
}

void ListPrint(LTNode* phead)//打印
{
	assert(phead);
	LTNode* cur = phead->next;
	while (cur != phead)
	{
		printf("\n考号：%s 姓名：%s 性别:%s 年龄:%s 报考类别:%s  \n", cur->id, cur->name, cur->sex, cur->age, cur->subject);
		cur = cur->next;
	}
}




void ListPushFront(LTNode* phead)//头插入
{
	assert(phead);
	LTNode* newnode = (LTNode*)malloc(sizeof(LTNode));
	assert(newnode);
	newnode->next = NULL;
	newnode->prev = NULL;
	printf("\n请依次输入考生的考号、姓名、性别、年龄及报考类别!\n");
	scanf("%s %s %s %s %s", &(newnode->id), &(newnode->name), &(newnode->sex), &(newnode->age), &(newnode->subject));
	LTNode* next = phead->next;
	phead->next = newnode;
	newnode->prev = phead;
	newnode->next = next;
	next->prev = newnode;
}


void ListPushBack(LTNode* phead)//尾插入
{
	assert(phead);
	LTNode* newnode = (LTNode*)malloc(sizeof(LTNode));
	assert(newnode);
	newnode->next = NULL;
	newnode->prev = NULL;
	printf("\n请依次输入考生的考号、姓名、性别、年龄及报考类别!\n");
	scanf("%s %s %s %s %s", &(newnode->id), &(newnode->name), &(newnode->sex), &(newnode->age), &(newnode->subject));
	LTNode* tail = phead->prev;
	tail->next = newnode;
	newnode->prev = tail;

	phead->prev = newnode;
	newnode->next = phead;
}


void ListPopFront(LTNode* phead)//头删除
{
	assert(phead && phead->next != phead);
	LTNode* next = phead->next;
	LTNode* nextNext = next->next;
	phead->next = nextNext;
	nextNext->prev = phead;
	free(next);
	next = NULL;
}

void ListPopBack(LTNode* phead)//尾删除
{
	assert(phead);
	LTNode* tail = phead->prev;
	LTNode* prev = tail->prev;
	phead->prev = prev;
	prev->next = phead;
	free(tail);
	tail = NULL;
}

LTNode* ListFind(LTNode* phead)//查找
{
	assert(phead);
	LTNode* cur = phead->next;
	char input[IMAX];
	printf("\n请输入要查找的报名学生学号：");
	scanf("%s", &input);
	getchar();
	while (cur!= phead)
	{
		if (strcmp(cur->id,input ) == 0)
		{
			printf("\n考号：%s 姓名：%s 性别:%s 年龄:%s 报考类别:%s  \n", cur->id, cur->name, cur->sex, cur->age, cur->subject);
			return cur;
		}
		cur = cur->next;
	}
	printf("没有该考生！");
	return NULL;
}


void ListChange(LTNode* pos)//确认是否改变数据
{
	LTNode* cur = pos;
	int input;
	printf("\n====请确定是否改变数据：1. change  0.exit ====\n");
	scanf("%d", &input);
	if (input)
	{
		LTNode* next = cur->next;//pos后的结构体
		LTNode* prev = cur->prev;//pos前的结构体
		LTNode* newnode= (LTNode*)malloc(sizeof(LTNode));
		printf("\n请依次输入考生的考号、姓名、性别、年龄及报考类别!\n");
		scanf("%s %s %s %s %s", &(newnode->id), &(newnode->name), &(newnode->sex), &(newnode->age), &(newnode->subject));
		prev->next = newnode;
		newnode->prev = prev;

		next->prev = newnode;
		newnode->next = next;
		free(cur);
	}
}
```

## 4.3 Text.c
```C
#define _CRT_SECURE_NO_WARNINGS 1
#include "System.h"

void menu()//确定进行的操作
{
	printf("\n***********欢迎进入考试查询系统*************\n");
	printf("****************操作列表********************\n");
	printf("***********1. 首行进行报名******************\n");
	printf("***********2. 尾行进行报名******************\n");
	printf("***********3. 删除首行报名学生数据**********\n");
	printf("***********4. 删除尾行报名学生数据**********\n");
	printf("***********5. 查询报名学生数据**************\n");
	printf("***********6. 更改报名学生数据**************\n");
	printf("***********7. 查询所有报名学生数据**********\n");
	printf("***********0. 退出考试查询系统**************\n");
}


int main()
{
	LTNode* plist = ListInit();//哨兵位头节点的创建
	LTNode* pos;
	int input = 0;
	do
	{
		menu();
		printf("请确认操纵:");
		scanf("%d", &input);
		switch (input)
		{
		case 1:
			ListPushFront(plist);//头插入
			break;
		case 2:
			ListPushBack(plist);//尾插入
			break;
		case 3:
			ListPopFront(plist);//头删除
			break;
		case 4:
			ListPopBack(plist);//尾删
			break;
		case 5:
			ListFind(plist);//查找
			break;
		case 6:
			pos = ListFind(plist);
			ListChange(pos);//改变数据
			break;
		case 7:
			ListPrint(plist);//打印
			break;
		case 0:
			printf("退出考试查询系统！\n");
			break;
		default:
			printf("输入错误，请重新输入\n");
			break;
		}
	} while (input);
	return 0;
}
```
